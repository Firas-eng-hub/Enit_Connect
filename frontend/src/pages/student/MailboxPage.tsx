import { MailboxView } from '@/features/mailbox/MailboxView';

export function MailboxPage() {
  return (
    <MailboxView
      title="Mailbox"
      subtitle="Communicate with students, companies, and administrators."
    />
  );
}
